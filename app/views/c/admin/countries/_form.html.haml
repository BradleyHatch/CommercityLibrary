= form_for @country do |f|
  = f.error_messages
  = tabs do

    = tab :delivery_zone, 'Delivery Zone' do
      .field
        = f.label :zone_id
        = f.select :zone_id, @zones

  .actions.form-page__actions
    = f.submit class: 'btn'

